# Dev-only API base URL. Production builds lock the API URL at build time.
# NO secrets ever belong in this file or in .env — tokens live in the OS keychain via safeStorage.
EPP_DEV_API_URL=http://localhost:8000
